/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Config file for defining DSL2 per module options and publishing paths
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Available keys to override module options:
        ext.args   = Additional arguments appended to command in module.
        ext.args2  = Second set of arguments appended to command in module (multi-tool modules).
        ext.args3  = Third set of arguments appended to command in module (multi-tool modules).
        ext.prefix = File name prefix for output files.
----------------------------------------------------------------------------------------
*/
/*
process {

    publishDir = [
        path: { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" },
        mode: params.publish_dir_mode,
        saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
    ]

    withName: CUSTOM_DUMPSOFTWAREVERSIONS {
        publishDir = [
            path: { "${params.outdir}/pipeline_info" },
            mode: params.publish_dir_mode,
            pattern: '*_versions.yml'
        ]
    }


    withName: 'NEAT' {
        
        publishDir = [
            path: { "${params.outdir}/neat" },
            mode: params.publish_dir_mode,
        ]
        ext.args = [
            '--bam',
            '--vcf',
            '--no-fastq'
        ].join(' ').trim()
    }


    withName: 'RANDOM_SITES' {
        
        publishDir = [
            path: { "${params.outdir}/bamsurgeon/random_sites" },
            mode: params.publish_dir_mode,
        ]
        ext.args = [
            '--avoidN'
        ].join(' ').trim()
    }


    withName: 'BAMSURGEON' {
        publishDir = [
            path: { "${params.outdir}/bamsurgeon" },
            mode: params.publish_dir_mode,
        ]
        ext.args = [
            '--ignoresnps',
            '--tagreads',
            '--ignorepileup',
            '--mindepth 8',
            '--maxdepth 100',
            '--minmutreads 4',
            '--aligner mem'
        ].join(' ').trim()
    }


    withName: 'VARDICTJAVA' {
        publishDir = [
            path: { "${params.outdir}/vardict" },
            mode: params.publish_dir_mode,
        ]
        ext.args = [
            '-c 1 -S 2 -E 3 -g 4',
            '-f 0.0001', 
            '--nosv',
            '-F 0x700'
        ].join(' ').trim()
        ext.args2 = [
            '-E',
            '-f 0.0001',
        ].join(' ').trim()
    }


    withName: 'MUTECT2' {
        publishDir = [
            path: { "${params.outdir}/mutect2" },
            mode: params.publish_dir_mode,
        ]
        ext.args = [
            '--min-base-quality-score 20',
        ].join(' ').trim()
    }


    withName: 'VARSCAN2' {
        publishDir = [
            path: { "${params.outdir}/varscan2" },
            mode: params.publish_dir_mode,
        ]
        ext.args = [
            '-d 0'
        ].join(' ').trim()
        ext.args2 = [
            '--min-var-freq 0.0',
            '--output-vcf'
        ].join(' ').trim()z
}
*/